<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.26.xsd">

    <changeSet id="009-create-reviews-table" author="hotel-booking-team">
        <createTable tableName="reviews">
            <column name="id" type="BIGSERIAL">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="user_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_reviews_user"
                             references="users(id)"/>
            </column>
            <column name="hotel_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_reviews_hotel"
                             references="hotels(id)"/>
            </column>
            <column name="booking_id" type="BIGINT">
                <constraints nullable="false" unique="true" uniqueConstraintName="uq_reviews_booking"
                             foreignKeyName="fk_reviews_booking" references="bookings(id)"/>
            </column>
            <column name="rating" type="SMALLINT">
                <constraints nullable="false"/>
            </column>
            <column name="comment" type="TEXT"/>
            <column name="created_at" type="TIMESTAMP" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <sql>
            ALTER TABLE reviews ADD CONSTRAINT chk_reviews_rating CHECK (rating >= 1 AND rating &lt;= 5);
        </sql>

        <createIndex tableName="reviews" indexName="idx_reviews_hotel_id">
            <column name="hotel_id"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
